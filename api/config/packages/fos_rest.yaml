fos_rest:
    body_listener: true
    body_converter:
        enabled: true
    format_listener:
        rules:
            - { path: '^/api', prefer_extension: true, fallback_format: json, priorities: [ json ] }
    view:
        view_response_listener: 'force'
        formats:
            json: true
